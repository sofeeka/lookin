import 'package:flutter/cupertino.dart';

class AddNewLookScreen extends StatelessWidget{
  const AddNewLookScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Add New Look"),
    );
  }
}